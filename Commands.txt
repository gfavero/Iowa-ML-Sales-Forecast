# Git Commands
git status
git add .
git commit -m "first commit"
git push

# Navigation (from Portfolio root)
cd .\projects\IOWASalesForecast\

# Python Virtual Environment
# (Localizado na raiz: c:\VS Code Files\Geraldo Portifolio)

# Criar (se precisar):
python -m venv .venv

# Activate (PowerShell - from ANY project folder):
..\..\.venv\Scripts\Activate.ps1

# Activate (Command Prompt):
..\..\.venv\Scripts\activate

# Install Requirements:
pip install -r projects\IOWASalesForecast\requirements.txt

# VS Code - Como conectar o notebook ao .venv:
# 1. No VS Code, abra o arquivo .ipynb
# 2. No canto superior direito, clique em "Select Kernel"
# 3. Escolha "Python Environments..." -> "Select Python Interpreter"
# 4. Selecione o que aponta para: .venv\Scripts\python.exe (na pasta RAIZ)
# 5. Se não aparecer, aperte o botão de "Refresh".
